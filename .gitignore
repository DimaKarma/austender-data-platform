# secrets
.env
profiles.yml
*.key
*.p8
*.pem

# dbt
austender_project/target/
austender_project/dbt_packages/
austender_project/logs/
logs/
# machine-local dbt telemetry id
austender_project/.user.yml

# python
__pycache__/
*.pyc
.venv/
venv/

# data (the 75MB CSV is not committed)
*.csv
*.csv.gz
# but dbt seeds are versioned reference data, not bulk source data
!austender_project/seeds/*.csv

# ABR bulk extract: ~944MB of archives and a ~2.6GB parsed CSV, re-downloadable
ingestion/abr_data/

# reference material / source documents — not part of the pipeline
*.pdf

# OS
.DS_Store

# personal: kid-friendly explainer (Russian), not part of the public repo
explain-to-kid.md

# personal: response to the independent audit (Russian), not part of the public repo
RESPONSE_TO_REVIEW.md
PROJECT_REVIEW.md
