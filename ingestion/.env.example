# Copy to .env and fill in. .env is gitignored — credentials are never committed.
# Account Identifier: Snowsight -> Admin -> Accounts -> copy button.
# Format: <orgname>-<account_name>, e.g. ABCDEFG-XY12345
SNOWFLAKE_ACCOUNT=ABCDEFG-XY12345

# The username you set on the "Create your account" step (NOT your email).
SNOWFLAKE_USER=YOUR_USERNAME

# Auth: set EITHER a password OR a key-pair path (key-pair preferred, and it keeps
# working if the account enforces MFA on password logins). To use key-pair:
#   openssl genrsa 2048 | openssl pkcs8 -topk8 -inform PEM -out dev_rsa_key.p8 -nocrypt
#   ALTER USER <you> SET RSA_PUBLIC_KEY='<public key body>';   -- in Snowflake
# then set SNOWFLAKE_PRIVATE_KEY_PATH below and leave SNOWFLAKE_PASSWORD blank.
SNOWFLAKE_PASSWORD=YOUR_PASSWORD
# SNOWFLAKE_PRIVATE_KEY_PATH=/path/to/dev_rsa_key.p8

SNOWFLAKE_ROLE=AUSTENDER_DE
SNOWFLAKE_WAREHOUSE=AUSTENDER_WH
SNOWFLAKE_DATABASE=AUSTENDER_DB
SNOWFLAKE_SCHEMA=BRONZE
